//Input: n = 5
// Output: 25
#include<bits/stdc++.h>
using namespace std;

int square(int n)
{
    if(n<0) n=-n;
    int res = n;
    for(int i=1 ; i<n ; i++)
        res+=n;
    return res;
}

int main()
{
    for(int n=1 ; n<=5 ; n++)
    {
        cout<<square(n)<<endl;
    }
    return 0;
}
