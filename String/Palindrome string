class Solution{
public:	
	
	
	int isPlaindrome(string S)
    {
    string s = "";
    for(int i=0 ; i<S.size() ; i++)
        s+=S[i];
    string s_c;
    s_c = s;
    reverse(s_c.begin() , s_c.end());
    if(s_c==s)  return 1;
    else    return 0;
    }

};
